---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ingress-network-policy-external-{{ .APP_NAME }}
spec:
  podSelector:
    matchLabels:
      name: {{ .APP_NAME }}
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-external
      ports:
      - port: 10080
        protocol: TCP
      - port: 10443
        protocol: TCP
